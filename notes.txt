add this


~centering text:
	horizontal -- text-align:center
	verticle -- display:flex; justify-content: center; align-items: center
	or
	padding-top

~ justify-content: space between (widest)
		   space around  (mid)
		   space evenly  (closeset)


$('li').on('click', event=>{
        event.stopPropagation();
        console.log ('li clicked');

$(this).next('section').toggleClass('secondary');
this
event.target no
event.currentTarget no
		.children()
		.next()
		.find()
				p yes
				section yes
				.secondary yes
$(this).find('section').slideToggle("fast");
$(this).find('section').toggleClass('secondary');  opens all

$(this).next('section').toggleClass('secondary') does nothing

$(this).nextAll('section').toggleClass('secondary'); does nothing

$(this).find('p').slideToggle("fast"); does nothing

$(event.currentTarget).find('section').slideToggle("fast")